## Installing the extension

Just run `npm install && npm run dev`; this will open Raycast and install the extension.
You can kill npm after that; the extension stays loaded.

Raycast extension docs [here](https://developers.raycast.com/basics/create-your-first-extension).
